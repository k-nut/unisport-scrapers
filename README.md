# unisport-scrapers

Scrapers for all sports classes offered by universities in Berlin.

## Installation

Ensure you have [uv](https://docs.astral.sh/uv/) installed.

## Running
You can run an individual scraper via `uv run scrapy crawl hu -o hu.json` or run a script that runs all scrapers 
by running `uv run run_all.sh`
